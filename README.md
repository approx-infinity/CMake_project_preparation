You can run each one by 
```bash
git clone https://github.com/Quntized/CMake_project_preparation.git
cd ~/CMake_project_preparation
cd <dedicated_you_want>
cmake -B build -S .
cmake --build build
./build/<executable_name>
```
That's just it for you to run.
